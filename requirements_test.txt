# pytest-homeassistant-custom-component is welded to one Home Assistant core version — this pin
# pulls homeassistant==2026.9.0b4 transitively. Bump both together (the HAcc release notes name the
# HA version each release targets).
pytest-homeassistant-custom-component==0.13.361
# hassil is what Home Assistant's conversation integration matches sentences with, and it is NOT a
# dependency of the core wheel — it is installed at runtime from that integration's manifest. Some
# tests feed the shipped custom_sentences/ YAML through the real recognize_best to prove the voice
# grammar behaves as designed (which sentence wins, and that each file is valid on its own); they
# are importorskip-guarded, so without this line they silently skip and the load-bearing claims go
# unchecked. Pinned to the version homeassistant/components/conversation/manifest.json requires for
# the core version above — bump it with the HAcc pin.
hassil==3.12.0
pytest-cov>=5.0.0
# Pinned exact: ruff's py314 formatter regression (astral-sh/ruff#23090, #25901) rewrites
# `except (A, B):` to the unparenthesized form. Freeze the version so a future ruff that
# reverses that behaviour can't turn `ruff format --check` red on an unrelated PR. Renovate
# bumps this deliberately (and we re-verify formatting when it does).
ruff==0.16.5
mypy>=1.11.0
